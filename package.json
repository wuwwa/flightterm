{
  "name": "flightterm",
  "version": "5.6.3",
  "description": "Flight-tracking terminal with a React dashboard, Express API, SQLite history, and FAA SWIM integration.",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev  --prefix backend",
    "dev:frontend": "npm run dev  --prefix frontend",
    "dev:swim": "node backend/swim-service.js",
    "predev": "node scripts/cleanup-ports.js",
    "dev": "concurrently --kill-others --names backend,frontend,swim --prefix-colors cyan,green,yellow \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:swim\"",
    "build": "npm run build --prefix frontend",
    "deploy": "fly deploy",
    "logs:prod": "fly logs"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
