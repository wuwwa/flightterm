{
  "name": "flightterm",
  "version": "5.0.0",
  "description": "Flight tracking terminal — React + Express. v5.0.0 adds the Correlation Layer: fire/weather/event/webcam/space-weather sources joined to live tracks to turn kinematic inferences into corroborated ones.",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev  --prefix backend",
    "dev:frontend": "npm run dev  --prefix frontend",
    "dev:swim": "node backend/swim-service.js",
    "predev": "node scripts/cleanup-ports.js",
    "dev": "concurrently --kill-others --names backend,frontend,swim --prefix-colors cyan,green,yellow \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:swim\"",
    "build": "npm run build --prefix frontend",
    "deploy": "fly deploy",
    "logs:prod": "fly logs"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
